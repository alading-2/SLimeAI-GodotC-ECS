---
title: 实体生命周期分配问题
status: 初稿
created: 2026-06-06
tags: [ecs, allocation, lifecycle, low]
description: 实体Spawn/Destroy过程中的集合分配和LINQ使用问题
---

# 实体生命周期分配问题

> 严重度：🟢 LOW（生命周期方法，非每帧）
> 影响范围：实体生成、销毁、组件注册

## 涉及代码（7处）

### 1. ComponentRegistrar — .Distinct().ToArray() 注册时

**文件：** `ECS/Runtime/Entity/Components/ComponentRegistrar.cs` 第44行
```csharp
foreach (var component in components.Distinct().ToArray())
```

### 2. HandleLifecycleChildrenOnDestroy — .ToArray()

**文件：** `ECS/Runtime/Entity/Manager/EntityManager.cs` 第483行
```csharp
var childSnapshot = _lifecycleTree.GetChildren(entityId).ToArray();
```

### 3. EntityManager_Component — new List\<Node\>()

**文件：** `ECS/Runtime/Entity/Components/EntityManager_Component.cs` 第142行
```csharp
IList<Node> componentsToRegister = new List<Node>();
```

### 4. DataValueConverter.ConvertForRead — Activator.CreateInstance

**文件：** `ECS/Runtime/Data/DataRuntimeStorage.cs` 第786, 860, 868行
```csharp
return clrType.IsValueType ? Activator.CreateInstance(clrType) : null;
return Activator.CreateInstance(targetType, systemVectorValue.X, systemVectorValue.Y);
```

### 5. Relationship.Data — Dictionary\<string, object\>

**文件：** `ECS/Runtime/Entity/LegacyRelationship/EntityRelationshipManager.cs` 第67行
```csharp
public Dictionary<string, object> Data { get; set; } = new();
```

### 6. RuntimeDataSnapshot.Manifest — Dictionary\<string, object?\>

**文件：** `ECS/Runtime/Data/RuntimeSnapshot/RuntimeDataSnapshot.cs` 第23行
```csharp
public Dictionary<string, object?> Manifest { get; init; } = new(StringComparer.Ordinal);
```

### 7. DataRuntimeStorage._computedCache — Dictionary\<string, object?\>

**文件：** `ECS/Runtime/Data/DataRuntimeStorage.cs` 第1085行
```csharp
private readonly Dictionary<string, object?> _computedCache = new(StringComparer.Ordinal);
```

## 严重度说明

以上问题都是生命周期方法（Spawn/Destroy/Load），非每帧调用。优化优先级低于DataSlot和EventBus问题。但如果战斗中大量实体频繁Spawn/Destroy（如子弹、特效），这些分配会累积。

## 解决方案

### 生命周期方法 — 对象池 + 预分配

```csharp
// ComponentRegistrar — 预分配List
private static readonly List<Node> _tempComponents = new(16);

// EntityManager.Destroy — 复用HashSet
private static readonly HashSet<string> _reusableVisited = new();

// Relationship.Data — 类型分区（与DataSlot方案一致）
public Dictionary<string, float> FloatData { get; set; } = new();
public Dictionary<string, int> IntData { get; set; } = new();
public Dictionary<string, string> StringData { get; set; } = new();
```