# 当前样本日志问题与整理方案

> 更新：2026-06-10
> 状态：current design note
> 样本：`.ai-temp/log-runs/20260610-013907/raw/scene-log.jsonl`

## 0. 本轮追问和复盘结论

本轮用户原始追问已追加到 `source-request.md`。核心问题不是“文档有没有列出需求”，而是：

> 需求全部写出来了，为什么让你重构 log 的时候没有完成目标？那你做了什么？为什么会这样？我明确要求你对打印信息整理，现在没有完成。

结论先说：用户判断成立。SDD-0040 T1 做成了 **结构化记录管道**，没有做完 **打印信息整理管道**。

| 层 | 当前状态 | 是否达到目标 | 说明 |
| --- | --- | --- | --- |
| Logger core | 已有 `LogEntry`、severity/outcome/validationStatus、runElapsed/frame/physicsFrame、sink、profile、budget | 部分达到 | 解决“怎么记录成结构化事实”，但不等于“AI 第一眼知道该看什么”。 |
| OperationTrace | Ability / Damage / TargetSelector / ObjectPool / Timer / System 有第一批 trace | 部分达到 | 能打 flow，但高频成功路径仍大量产生 flow completion；缺 duration、reasonCode、owner 契约门禁和聚合规则。 |
| ValidationSession | 类型和 artifact 写入能力存在 | 部分达到 | 样本 run `validationEntries=0`、`artifacts=0`，说明承载场景没有把验证事实接入主链路。 |
| `logctl analyze` | 能拆 raw、by-owner、by-phase、flows、noise、missing-fields、gate-report、ai-context | 不达标 | 目录存在，但 markdown digest 缺失，`ai-context.md` 太薄，gate 状态误报为 `passed`。 |
| Owner Log 文档 | 第一批 owner 文档存在 | 不达标 | 文档没有反向约束调用点字段，也没有被 analyzer 用来判断 owner 缺字段 / 缺 phase / 缺 reasonCode。 |
| 运行样本验收 | 有真实样本 `.ai-temp/log-runs/20260610-013907` | 不达标 | 没有把真实样本作为完成门禁，只用“产物存在”和合成样本验证，导致目标偏离。 |

因此不能再把 SDD-0040 描述成“只剩 Godot scene smoke blocker”。更准确的状态是：

```text
T1: Logger core / 最小 analyzer / 第一批 flow / 文档入口已完成。
T2: analyzer digest / flow 边界 / semantic missing-fields / owner 降噪 / Validation gate 仍未完成。
Final gate: Godot scene smoke 仍受承载游戏 runner 阻塞。
```

## 0.1 本轮搜索范围

本轮没有使用 Web / Context7。原因是这次失败点不依赖外部 API 或第三方库版本，本地事实源足以证明问题。已检查范围：

- 样本 raw：`.ai-temp/log-runs/20260610-013907/raw/scene-log.jsonl`
- 样本 analysis：`.ai-temp/log-runs/20260610-013907/analysis/**`
- Log CLI：`Workspace/Tools/logctl/logctl.mjs`
- Logger core：`Src/ECS/Tools/Logger/Log.cs`
- Validation：`Src/ECS/Tools/Logger/ValidationSession.cs`
- 高频 owner 调用点：`TargetQueryEngine.cs`、`ObjectPool.cs`、`DamageService.cs`、`HealthBarUI.cs`
- 设计事实源：`design/Tool/10.Log/*`、`DocsAI/ECS/Tools/Logger/README.md`
- SDD 状态：`sdds/029-SDD-0040-log-ai-first-observation-hard-cutover/{README.md,tasks.md,progress.md,bdd.md,notes.md}`

未覆盖范围：

- 未重新跑 Godot 场景；当前问题可由已有样本复查，不需要先解除 runner blocker。
- 未全面审计所有 `_log.Info(...)` 调用点；本轮只按样本 top owner 设计 T2 顺序。

## 0.2 样本复查关键数字

本轮用 Node 逐行解析 raw JSONL，排除最后一行截断 JSON 后得到：

| 指标 | 数值 | 含义 |
| --- | ---: | --- |
| raw 非空行 | 4915 | 最后一行是截断 JSON。 |
| 可解析 entries | 4914 | analyzer 当前把 invalid line 也作为 diagnostic entry 计入 4915。 |
| invalid JSONL | 1 | `gate-report` 未把它升为明显输入质量问题。 |
| `fields:{}` | 1109 | envelope 有了，业务字段仍缺。 |
| `operation == context` | 1109 | 很多旧调用仍没有稳定业务 operation。 |
| `channel=Flow` | 3730 | flow 过多，说明高频成功路径没有真正聚合。 |
| 现有 `flows` 规则命中 | 4914 | `logctl` 代码使用 `channel=flow || entry.operation`，把所有普通 operation 也归入 flows。 |
| Validation entries | 0 | 没有结构化验证事实。 |
| artifacts | 0 | 没有 artifact 主事实源。 |

Top owner/context/operation：

| owner/context/operation | count | 结论 |
| --- | ---: | --- |
| `TargetSelector/TargetQueryEngine/TargetQueryEntities` | 3041 | 高频查询成功路径被逐次 flow 输出，应该改为 owner summary / sample / aggregate。 |
| `ObjectPool/ObjectPool/ObjectPoolAcquire` | 271 | 租借成功路径应按 poolName 聚合。 |
| `Runtime/HealthBarUI/HealthBarUI` | 256 | 旧文本日志，且三条成功事件重复 83 次。 |
| `ObjectPool/ObjectPool/ObjectPoolRelease` | 179 | 回收成功路径应 batch summary。 |
| `Damage/DamageService/DamageProcess` | 111 | DamageProcess 有价值，但需要 processor chain digest，不只是 completed 计数。 |

这组数字说明，当前失败不是“日志太少”或“JSON 格式错”，而是 **高频结构化事件没有被再加工成 AI-first 分析入口**。

## 0.3 为什么没有完成目标

### 0.3.1 原始目标被拆错了

用户要求的是“对打印信息整理”，目标应包含三层：

1. **记录层**：把事实写成结构化 `LogEntry`。
2. **整理层**：把 raw JSONL 整理成 `summary.md / ai-context.md / noise / missing-fields / flows / failures`。
3. **判断层**：让 AI 能基于 artifact 判断 Code issue / Test issue / Log gap / Runner issue / Profile issue。

实际执行时主要完成了第 1 层和第 2 层的目录雏形，却把“目录存在”误当成“整理完成”。这就是根因。

### 0.3.2 做了什么

确实落地的内容：

- `Log.cs` 新增结构化 envelope、sink abstraction、C# stdout summary、buffered JSONL file、memory/artifact sink、optional Godot editor sink。
- `LogSeverity / LogOutcome / LogValidationStatus` 拆分，旧 `Success` 兼容映射为 outcome。
- `Config/Log/log.profile.json`、`log.rules.json`、`log.overrides.json` 和 run metadata 存在。
- `OperationTrace` 能写 `channel=Flow` 的 start/step/complete。
- `ValidationSession` 能写 checks 和 artifact。
- `logctl analyze/query/ingest/suggest/profile show` 最小可用。
- 第一批 owner 文档和 README 已写。

这些有价值，但只证明“管道能跑”，不证明“输出已经可读、可控、可判断”。

### 0.3.3 没做完什么

没做完的内容：

- `analysis/summary.md` 没生成。
- `ai-context.md` 只列 runDir、resultSource、status、entries、owners、query 示例，没有 top noise、semantic missing fields、flow digest、owner 文档、下一步建议。
- `noise/summary.json` 只是 JSON 数组，没有 `noise/top-contexts.md`、dedupe 建议和 owner action。
- `missing-fields/missing-fields.json` 只检测 required envelope 缺字段，漏掉 `fields:{}`、`operation==context`、缺 `entityId/reasonCode/durationMs/sourceFile` 等语义缺口。
- `flows/flows.json` 既包含真正 Flow，也包含普通 operation；同时真正 Flow 本身也太高频，没有汇总成 process digest。
- `gate-report.json` 把没有 validation/artifact 且没有 failure 的 run 写成 `passed`，这误导了完成判断。
- `logctl suggest` 按完整 message 输出建议，导致 `HealthBarUI` 这类同 owner/context/operation 的重复事件没有先合并。
- Owner `Log.md` 没有成为 analyzer 的字段契约输入；文档和调用点没有闭环。
- SDD-0040 的 tasks/progress 没把这些 follow-up 变成未完成任务，导致状态看起来像“完成但缺 runner”。

### 0.3.4 为什么会这样

流程上有五个具体错误：

1. **验收口径过低**：T1.5 只要求生成 `analysis/raw/by-owner/by-phase/flows/failures/noise/missing-fields/ai-context.md`，没有规定 markdown digest 的信息质量。
2. **合成验证替代了真实样本验收**：合成 run 能证明 CLI 不崩，但不能证明 4915 行真实样本能被 AI 消费。
3. **把结构化等同于整理**：`LogEntry` 让日志机器可读，但不自动提供 top noise、流程摘要、缺字段任务和 gate 可信度。
4. **flow 概念过宽**：实现上 `channel=Flow` 和 `operation` 的边界没守住；设计上也没有限制高频成功路径的 trace 频率。
5. **SDD 状态没有反映质量缺口**：T1 完成后没有新增 T2 analyzer/owner follow-up，导致后续会话容易继续从错误完成态恢复。

## 0.4 下一步怎么完成

下一步不应该直接“少打几行日志”。要按 T2 切片完成整理闭环：

```text
T2.1 analyzer digest contract
  -> summary.md / ai-context.md / top noise / gate confidence
T2.2 gate status semantics
  -> passed vs no-failure-observed vs stdout-pattern-fallback
T2.3 flow boundary and aggregation
  -> 只把真实 flow 纳入 flows；高频成功路径 sample/aggregate
T2.4 semantic missing-fields
  -> 按 owner 输出 Log gap，而不是只查 envelope 缺字段
T2.5 owner hot-spot cleanup
  -> TargetSelector/ObjectPool/HealthBarUI/Damage/System 第一批调用点补字段和预算
T2.6 Validation artifact adoption
  -> 样本场景没有 artifact 时不能报告通过
T2.7 verification and SDD state sync
  -> 用当前样本复跑 analyze，更新 SDD tasks/progress/bdd
```

### T2.1 `logctl analyze` 输出 markdown digest

修改位置：`Workspace/Tools/logctl/logctl.mjs`

新增或重构函数：

- `buildRunDigest(run, gateReport)`
- `buildTopOwners(entries)`
- `buildTopOperations(entries)`
- `buildSemanticMissingFields(entries)`
- `writeSummaryMd(outDir, digest)`
- `writeNoiseMd(outDir, digest)`
- `writeMissingFieldsMd(outDir, digest)`
- `writeFlowsIndexMd(outDir, digest)`
- `writeAiContextMd(outDir, digest)`

目标产物：

```text
analysis/
  summary.md
  ai-context.md
  gate-report.json
  noise/
    summary.json
    top-contexts.md
  missing-fields/
    missing-fields.json
    index.md
  flows/
    flows.json
    index.md
  failures/
    failures.json
    index.md
```

`summary.md` 必须第一屏回答：

- 这次 run 的 gate 状态和可信度。
- 结果来源是 artifact、structured validation、structured log、stdout fallback，还是 no-failure-observed。
- entries / invalid JSONL / validationEntries / artifacts / structuredFailures。
- top owner、top operation、top phase、top noise。
- 是否存在 `fields:{}`、`operation==context`、unknown owner/phase、缺 source。
- AI 下一步先读哪些文件，不读哪些 raw。

`ai-context.md` 必须面向下一步定位：

- failure focus，没有失败时必须写“没有 validation/artifact，因此只能 no-failure-observed”。
- top noisy owner/context/operation，并给动作：`budget` / `sample` / `aggregate` / `owner field contract` / `move to Validation`。
- flow digest，只展示真正 flow summary 和异常 flow。
- semantic missing-fields digest。
- owner 文档链接。
- 下一轮 query 和 profile override 建议。

### T2.2 gate 状态语义修正

修改位置：`Workspace/Tools/logctl/logctl.mjs`

当前错误：

```text
resultSource=structured-log
status=passed
validationEntries=0
artifacts=0
```

目标状态：

| status | 条件 | 含义 |
| --- | --- | --- |
| `passed` | artifact pass 或 Validation channel 明确 pass，且没有 structured failure | 行为被验证通过。 |
| `failed` | artifact fail、Validation fail、structured error/fail | 行为或运行失败。 |
| `no-failure-observed` | 有 structured log，但没有 Validation/artifact，也没有失败 | 没观察到失败，不等于通过。 |
| `stdout-pattern-fallback` | 只能靠 legacy stdout pattern | 低可信过渡结果。 |
| `invalid-input` | raw JSONL 截断或关键 artifact 解析失败 | 输入质量问题，需要修 runner/sink。 |

本样本应至少从 `passed` 改为 `no-failure-observed`，并把最后一行 invalid JSONL 写进 `summary.md`。

### T2.3 flow 边界和聚合

修改位置：

- `Workspace/Tools/logctl/logctl.mjs`
- `Src/ECS/Tools/Logger/Log.cs`
- 高频 owner 调用点：`TargetQueryEngine.cs`、`ObjectPool.cs`、`DamageService.cs`

规则：

- `flows/index.md` 只纳入 `channel=Flow` 且具备 flow 语义的 entry。
- 普通 `operation` 不能进入 flow。
- `OperationTrace` complete 应补 `durationMs`、`stepCount`、`reasonCode`、`budgetKey`、可选 `entityId/targetId`。
- 高频函数不要每次成功都输出完整 flow；默认应按 owner rule 采样或聚合。
- analyzer 应按 `correlationId` 聚合 start/step/complete，无法聚合时分类为 `Log gap`。

TargetSelector 的默认策略：

- 每次查询可以写 JSONL sample，但 stdout 不应逐次显示。
- 每个窗口输出 `TargetQuerySummary`，包含 queryCount、candidate min/max/avg、returned min/max/avg、truncatedCount、warningCount、suppressedCount。
- 失败或 warning query 保留完整 flow。

ObjectPool 的默认策略：

- Acquire / Release 成功路径按 poolName 聚合。
- 容量丢弃、重复释放、不活跃对象释放保留 warn/error 明细。
- Release 在场景关闭尾部应输出 batch summary，而不是逐个 completed。

Damage 的默认策略：

- `DamageProcess` 不应只有 completed；必须能看 processor chain digest：processorCount、blockedProcessor、baseDamage、finalDamage、actualDamage、isDodged、isCritical、reasonCode。
- 成功路径可以保留 JSONL summary；失败/阻断路径保留完整 fields。

HealthBarUI 的默认策略：

- `OnUnitCreated called`、`准备绑定`、`成功绑定` 三条成功文本合并为 `HealthBarBind` flow 或 bind summary。
- 成功路径默认只计数；失败绑定输出 error + entityId + reasonCode。

### T2.4 semantic missing-fields

修改位置：

- `Workspace/Tools/logctl/logctl.mjs`
- owner `Log.md` 或 README `## Log`

`missing-fields` 不能只检测 envelope required fields。它要输出“为什么 AI 不能判断”的任务。

建议检测：

| 检测 | 样本命中 | 归类 |
| --- | --- | --- |
| `fields:{}` | 1109 | Log gap |
| `operation == context` | 1109 | Log gap |
| flow complete 缺 `durationMs` | 当前 `OperationTrace` 没写 | Log gap / Log CLI issue |
| runtime 文本缺 `entityId` | HealthBarUI / Entity / System 多处 | Owner field gap |
| fail/warn 缺 `reasonCode` | 需按 owner 检测 | Owner field gap |
| missing `sourceFile/sourceLine` | 当前多数为空 | 可选 source gap |
| unknown owner/phase | analyzer 需统计 | Routing gap |

输出格式：

```text
missing-fields/index.md
  ## Runtime / HealthBarUI
  - 问题：operation==context，fields 为空，无法按 entity 聚合 bind 过程。
  - 样本：83 x OnUnitCreated / 83 x 准备绑定 / 83 x 成功绑定。
  - 任务：补 HealthBarBind operation，fields={entityId, entityType, poolName, outcome, reasonCode}。
```

### T2.5 owner hot-spot cleanup 顺序

只按样本 top 噪声先做第一批，不全仓大改：

1. `TargetSelector/TargetQueryEngine`
   - 把每次 completed 改为 JSONL sample + aggregate summary。
   - 补 query shape、candidate counts、filter counts、result counts、reasonCode。
2. `ObjectPool/ObjectPool`
   - Acquire / Release 成功路径按 poolName 聚合。
   - Release batch summary 支持尾部大量回收。
3. `Runtime/HealthBarUI`
   - 三条重复文本合并为 bind summary。
   - 失败保留 error fields。
4. `Damage/DamageService`
   - 补 processor chain digest。
   - 阻断、闪避、暴击、最终扣血字段固定。
5. `System/SystemManager`
   - 系统状态报告输出为一次 snapshot artifact/flow，不逐行刷 `MountGroup/IsEnabled`。

这批完成后，再决定是否扩展 Ability、Entity、Timer、Movement。

### T2.6 Validation artifact 接入

修改位置：

- `Src/ECS/Test/GlobalTest/MainTest/MainTest.cs` 或对应承载样本场景
- `Src/ECS/Tools/Logger/ValidationSession.cs`
- `Workspace/Tools/logctl/logctl.mjs`
- godot scene runner wrapper

规则：

- 只要用户把 run 当成验证，必须有 artifact 或 Validation channel。
- 没有 artifact 时，`gate-report.status` 不能是 `passed`。
- artifact 至少包含 expectedInputs、expectedObservations、passCriteria、failCriteria、checks、failures。
- 旧 `PASS/FAIL` 文本只作为 fallback，并在 report 中降级显示。

### T2.7 验收门禁

用当前样本作为第一验收，不先依赖新 Godot run：

```bash
node --check Workspace/Tools/logctl/logctl.mjs
Workspace/Tools/logctl/logctl analyze --run-dir .ai-temp/log-runs/20260610-013907 --out .ai-temp/log-runs/20260610-013907/analysis-next
Workspace/Tools/logctl/logctl query --analysis-dir .ai-temp/log-runs/20260610-013907/analysis-next owner=TargetSelector operation=TargetQueryEntities --format md
Workspace/Tools/logctl/logctl suggest --run-dir .ai-temp/log-runs/20260610-013907 --dry-run
python3 Workspace/SDD/sdd.py validate SDD-0040
python3 Workspace/SDD/sdd.py validate PRJ-0002
git diff --check -- SDD/project/projects/PRJ-0002-ecs-framework-refactor Workspace/Tools/logctl DocsAI/ECS/Tools/Logger
```

验收条件：

- `analysis-next/summary.md` 存在，并能一屏说明 status、confidence、top noise、invalid JSONL 和下一步。
- `analysis-next/ai-context.md` 不再只有 metadata 和 query 示例。
- 本样本 status 不再写成 `passed`，至少是 `no-failure-observed` 或包含 invalid-input warning。
- `flows/index.md` 不再把普通 operation 当 flow。
- `missing-fields/index.md` 明确列出 `fields:{}`、`operation==context` 和第一批 owner 字段任务。
- `noise/top-contexts.md` 对 TargetSelector、ObjectPool、HealthBarUI、Damage 给出合并/采样/字段补齐建议。
- `suggest --dry-run` 合并同 owner/context/operation 的重复 message，不再给 3 条 HealthBarUI 重复建议。
- SDD-0040 tasks/progress 明确 T2 follow-up 未完成，不能表达为 100% done。

## 0.5 默认假设和需要确认

默认假设：

- 不引入 Serilog、OpenTelemetry、Loki 等外部平台；继续用本地 `logctl` 和当前 JSONL。
- 第一目标是“AI 可消费分析目录”，不是先全仓清理所有 `_log.Info`。
- 当前样本就是 T2 第一验收样本。
- `passed` 必须只保留给 artifact / Validation 明确通过的 run。
- 高频成功路径默认 summary-only stdout，JSONL sample/aggregate；失败和 Validation 不被普通预算压掉。

需要确认：

- 是否接受把 SDD-0040 从“12/12 完成，只剩 Godot blocker”修正为“12/18，T2 analyzer/owner 整理未完成”？默认我会修正，因为这是事实状态。
- 是否接受先做 analyzer digest，再做 owner 调用点降噪？默认先 analyzer，因为它能立刻让现有样本可读，也能防止后续 owner 修改没有验收入口。
- `TargetSelector` 高频查询是否允许默认不记录每次成功 flow，只保留 sample/aggregate？默认允许；否则 JSONL 仍会在长 run 中快速膨胀。

## 1. 真实问题

本轮样本证明：当前方向不是“JSON 错了”，而是 **JSONL 还没有被整理成 AI 可分析的 artifact**。

`scene-log.jsonl` 已经有 `runElapsedMs / frame / physicsFrame / severity / owner / context / operation / phase / fields` 这些 envelope 字段，这比旧 `GD.PrintRich` 文本强很多。但 AI-first 的目标不是“把每行打印变成 JSON”，而是让 AI 可以稳定回答：

- 这次 run 是否真的通过，依据是 artifact、structured validation，还是 fallback？
- 问题发生在哪个 phase、owner、operation、entity 或 flow？
- 哪些日志是重复噪声，应该合并、采样或只进 JSONL？
- 哪些字段缺失导致无法判断？
- 下一轮应该打开哪个 owner、关闭哪个 context、补哪个 `Log.md`？

如果这些答案仍要 AI 从 4914 行 raw JSONL 里临场归纳，那么只是把“全量 stdout 难读”换成“全量 JSONL 难读”。

## 2. 样本证据

本轮检查范围：

```bash
wc -l .ai-temp/log-runs/20260610-013907/raw/scene-log.jsonl
jq -r '.severity' .ai-temp/log-runs/20260610-013907/raw/scene-log.jsonl
jq -r '.owner' .ai-temp/log-runs/20260610-013907/raw/scene-log.jsonl
Workspace/Tools/logctl/logctl analyze --run-dir .ai-temp/log-runs/20260610-013907 --out .ai-temp/log-runs/20260610-013907/analysis
Workspace/Tools/logctl/logctl suggest --run-dir .ai-temp/log-runs/20260610-013907 --dry-run
```

关键事实：

| 观察 | 证据 | 判断 |
| --- | --- | --- |
| raw 体量已经超出 AI 直接阅读范围 | `scene-log.jsonl` 约 4914 行；`logctl analyze` 解析出 4915 条，最后一行是 invalid JSONL diagnostic | raw 只能作为证据源，不能作为默认提示词输入。 |
| severity 几乎不能降噪 | `Info` 约 4163 条，`Debug` 约 665 条，`Warn` 约 86 条 | 只调 level 会隐藏信息，不能解决重复 owner / operation。 |
| 噪声高度集中 | `TargetSelector/TargetQueryEngine/TargetQueryEntities` 约 3041 条；`ObjectPoolAcquire` 约 271 条；`ObjectPoolRelease` 约 179 条 | 应优先按 owner/operation 做预算、summary 和 query，而不是全局关日志。 |
| fields 仍大量为空 | 约 1109 条 `fields:{}` | JSON envelope 有了，但业务字段不足，AI 仍要读 message。 |
| operation 经常只是 context 复制 | 约 1109 条 `operation == context` | 这不是稳定业务操作名，后续应补 owner `Log.md` 和调用点契约。 |
| 当前 analyzer 产物偏机器 JSON | 有 `by-owner/*.jsonl`、`by-phase/*.jsonl`、`noise/summary.json`、`missing-fields/missing-fields.json`，但没有 `summary.md`、`noise/top-contexts.md`、`missing-fields/index.md` | 产物已经开始拆分，但还不是 AI 默认可读入口。 |
| `ai-context.md` 太薄 | 只列 runDir、resultSource、status、entries、owners 和两个 query 示例 | 它没有回答 top noise、missing fields、flow digest、owner 文档和下一轮建议。 |
| flow 识别过宽 | `flows/flows.json` 当前包含几乎所有有 `operation` 的 entry | flow 应只来自 `channel=Flow`、`entryType` 或明确 OperationTrace 标记，不能把普通 operation 全算 flow。 |
| 样本没有 Validation 事实 | `validationEntries=0`、`artifacts=0`，gate 结果来自 `structured-log` | “passed” 只能说明没有 structured failure，不等价于验证通过。 |

## 3. 为什么会感觉更麻烦

用户的感觉成立。当前 JSONL 让日志变得“机器可解析”，但同时暴露了新问题：

1. **行数没有减少**：4914 行 raw JSONL 比纯文本更长，直接读更累。
2. **字段重复但语义不够**：每行都有 envelope，但 `fields:{}` 和 `operation == context` 让 AI 仍然要读自然语言 message。
3. **过程没有真正聚合**：`ObjectPoolRelease completed` 连续刷几十行，仍然需要 analyzer 合并成一个 flow/noise 摘要。
4. **分析入口缺失**：`ai-context.md` 没有给 AI 足够的第一屏结论，AI 只能回到 raw。
5. **测试事实不足**：没有 Validation artifact 时，AI 只能说“没有看到失败”，不能说“行为符合预期”。

结论：**JSON 是基础格式，不是最终可读结果。AI-first Log 必须把 raw JSONL 再整理成可消费的分析目录。**

## 4. 解决方向

### 4.1 `logctl analyze` 必须生成 AI 入口文件

目标目录必须至少包含：

```text
analysis/
  summary.md
  ai-context.md
  gate-report.json
  raw/
  by-owner/
  by-phase/
  flows/
    index.md
    <Owner>/<Operation>.md
  failures/
    index.md
  noise/
    top-contexts.md
    suppressed.md
  missing-fields/
    index.md
```

`summary.md` 面向人和 AI 的第一眼总览，必须回答：

- run status 和 resultSource。
- entries / validationEntries / artifacts / structuredFailures。
- top owners / top phases / top noisy operations。
- 是否存在 invalid JSONL、missing required fields、unknown owner/phase。
- 这次 run 应该先读哪些 owner 文档和哪些 query。

`ai-context.md` 必须比 `summary.md` 更面向下一步分析，包含：

- failure focus。
- top noise with recommended action。
- flow digest。
- missing field digest。
- owner `Log.md` 链接。
- 需要补日志、调 profile、修测试还是修代码的初步分类。

### 4.2 flow 不能等于 operation

当前 `flows/flows.json` 把普通 `operation` 也纳入 flow，会制造假 flow。正确规则：

- `channel=Flow` 可以进入 flow。
- 或 `entryType=flow_summary|flow_step|flow_suppressed_summary` 可以进入 flow。
- 或 `fields.flowId / correlationId + outcome + durationMs` 满足完整 OperationTrace 契约时进入 flow。
- 普通 runtime log 的 `operation` 只能进入 `by-owner` / `by-phase` / `noise`，不能自动进入 `flows`。

### 4.3 noisy context 要给可执行建议

样本中的优先 owner：

| owner/context/operation | 样本问题 | 建议 |
| --- | --- | --- |
| `TargetSelector/TargetQueryEngine/TargetQueryEntities` | 约 3041 条，远超其他日志 | 默认 stdout 只输出异常和 summary；JSONL 采样；TargetQuery flow summary 记录候选数、过滤数、结果数、reasonCode。 |
| `ObjectPool/ObjectPool/ObjectPoolAcquire` | 约 271 条 | `PoolGetReleaseActivate` flow 聚合，按 poolName 预算；连续成功路径默认 summary-only。 |
| `ObjectPool/ObjectPool/ObjectPoolRelease` | 约 179 条，尾部出现同一帧大量 release | 合并成 release batch summary，保留 active/idle min/max/count。 |
| `Damage/DamageService/DamageProcess` | 约 111 条 | DamageProcess 保留完整 processor chain，但 stdout 只显示失败或异常 summary。 |
| `Runtime/HealthBarUI/HealthBarUI` | 同一 enemy created/bind/success 三条重复 83 次 | 合并为 UI bind flow 或只在失败时输出，成功路径计数即可。 |

### 4.4 missing-fields 要升级为 owner 任务

`missing-fields` 不能只列 JSON 数组。它要按 owner 输出：

- 哪些字段缺失。
- 哪些 message 只能靠自然语言判断。
- 对应 owner 应补的 `Log.md` 字段契约。
- 这属于 `Log gap`、`Profile issue` 还是 `Code issue`。

样本里 `fields:{}` 和 `operation == context` 很多，这不一定是代码 bug，但它是 **Log gap**。

### 4.5 Validation artifact 是通过判断的必要条件

样本 `resultSource=structured-log` 且 `validationEntries=0`、`artifacts=0`。这类 run 只能表达：

```text
没有发现 structured error/fail。
```

不能表达：

```text
测试行为已经通过验收。
```

因此 analyzer 的 gate report 应区分：

| 状态 | 含义 |
| --- | --- |
| `passed` | artifact 或 Validation 明确通过。 |
| `no-failure-observed` | 有 structured log，但没有 Validation artifact，只能说明没看到失败。 |
| `stdout-pattern-fallback` | 只能靠 legacy stdout pattern 判断，低可信。 |

## 5. 写入设计和实施的 TODO

- TODO：升级 `Workspace/Tools/logctl/logctl.mjs analyze`，补 `summary.md`、`noise/top-contexts.md`、`missing-fields/index.md`、`flows/index.md`。
- TODO：修正 flow 识别规则，不再把所有有 `operation` 的 entry 当 flow。
- TODO：`ai-context.md` 增加 top noise、missing field、flow digest、owner docs、下一轮 query 和问题分类。
- TODO：`logctl suggest` 合并相同 owner/context/operation 的多条 message，避免同一 HealthBarUI 给出重复建议。
- TODO：为 `TargetSelector`、`ObjectPool`、`Damage`、`Runtime/UI` 第一批补 owner `Log.md` 或 README `## Log`。
- TODO：Validation scene 必须输出 artifact；没有 artifact 的 run 在 gate report 中标记为 `no-failure-observed`，避免误报“已验证通过”。
