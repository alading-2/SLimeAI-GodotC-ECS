
## 问题

## Data
更新文档，检查

## Now
### 1
@TestSystem @FeatureSystem
- TestSelectionContext.Events应该放在TestSystem，专门是TestSystem的事件
- TestSystem.RegisterModulesFromScene排序是否有必要？我认为安装字母名称顺序随便排序或者不排序都行
- 现在要给TestSystem的测试模块分组，测试模块里面写分组，属于哪个分组，比如测试系统可以分属性、技能分组，分组可以多重嵌套，分组跟FeatureId_Ability一样，比如技能.测试1.测试2，那就是技能是第一分组，测试1是第二分组，测试2是测试模块名字，新建个字段在ITestModule里面，参考FeatureSystem，根据这个自动在UI里面分组
- TestSystem选择测试模块下拉列表太长了，也要分组，深度思考怎么做，我认为选择测试模块的UI要重构，可以放在左边，可以随时隐藏，需要的时候才显示，深度思考不一定我说的这样，反正最终结果要我满意，要符合常理，继续放上面也是可以的，反正分组要做好，要自然
- ITestModule我很奇怪，位深不直接用FeatureSystem的生命周期统一管理，而是自己写新的，重复造轮子
- 刷新调度有关逻辑过度设计太过复杂都不知道在写什么，需要刷新直接刷不就好了？搞那么多干什么，TestModuleBase、TestRefreshScheduler等
- 以资深游戏开发者深度思考详细分析，广泛搜索web,mcp，其他游戏框架只是参考，不能照抄，要适合当前GodotC#ECS框架

### 2
- Src/ECS/Base/System/TestSystem Src/ECS/Base/System/Spawn Src/ECS/Test/SingleTest/ECS/System/Spawn Data/ResourceManagement/ResourcePaths.cs Data/Data/Unit
  新加一个可以自定义生成敌人的测试，我认为可以参考SpawnSystem测试场景，要可以选择单位，Data里面有单位的数据.tres，深度思考怎么做
  其实获取所有当前系统的技能、单位、特效等这个是一个通用的，看看要做成什么工具，比如War编辑器里面就是可以选择不同类型的单位，我感觉可以通过获取tres数据然后分析获得所需的所有单位/特效/技能
  等，然后通过数据进行分类，选择之类的，深度思考详细分析广泛搜索,web,mcp
  使用superpowers详细规划，搜索可以放到subagent,只提取核心信息关键结论节省token
---
$brainstorming  $using-superpowers
Src/ECS/Base/System/TestSystem   Data/ResourceManagement
- 测试系统新增查看对象池信息测试模块，分组是信息，可以查看对象池详细信息，可以详细到某个池的信息，当前数量、最大数量等参数
- 深度思考详细分析有什么测试信息是符合本项目框架适合让开发者在游戏中查看的
- 我想做一个能够批量查看视觉场景CharactorBody2D的测试模块，我不确定要不要做成测试模块，还是说单独的测试场景，用途：将某个类型，比如单位、特效，可以按照分组筛选，比如可以选单位-玩家，单
  位-敌人等，反正分组之前应该有做的，Data/ResourceManagement，这个是用来将这些视觉场景实例化，放在地图上查看效果，可以统一播放某个动作，看看实际效果怎么样，这个用途应该很常见，或者做成
  插件更好？
-

## 测试系统
@Src/ECS/Base/System/AbilitySystem @Data/DataKey/Attribute 
我需要做测试系统，在Src\ECS\System下面新建文件夹，用于游戏运行中的测试，包括属性、技能等功能
- 测试系统需要做成通用，后面还要加很多测试功能，增加起来要方便，深度思考怎么做
- 增加鼠标选择Entity功能，在游戏当中，鼠标点击能够选择一个Entity，这个也暂时放在测试系统，用于测试系统选择entity
- 用一个按钮（测试）点击，在左上角显示，然后有下拉列表，选择对应的测试功能，，比如属性测试功能就能设置属性，比如攻击、生命、是否无敌等属性
- 技能测试，自由添加删除技能，左边是所有技能分类；右边是当前有的技能，可以右键禁用技能
我可能表达不全，
深度思考详细分析


## TODO
- [x] 技能逻辑复用，比如特效、伤害这种可以写成工具不用次次都重新，AbilityConfig.cs增加参数，比如伤害类型等
- [] 伤害是选择工具遍历entity距离，还是用碰撞事件？特效、武器、子弹都要一般都要用碰撞。
- [] 测试系统按一个按钮显示/不显示测试UI
- [] 检查技能逻辑、测试系统
- [] 回旋镖等很多运动技能都很有问题
- [] AbilityConfig.cs顺序
- **要不要用行为树状态机来管理运动系统，比如运动结束时自动切换成优先级高的一种状态？不对，运动跟状态是两个概念，比如眩晕的时候可以运动**
- 状态管理，比如无敌，如果有多个技能设置无敌，应该要区分，比如现在有两段无敌，第一段无敌结束后，第二段无敌应该继续生效，如果第一段无敌结束，就结束无敌了？现在单纯设置DataKey就是这样。要不要搞个状态机来管理？怎么管理？
- **更新运动系统所有说明文档，尤其是一大堆<c></c>感觉不好，深度思考**

- []【严重问题】Dash技能我把攻击力设置成1000，这个关系应该不大，主要是伤害敌人的数量有十几个就卡？现在又好像没有了？
- 
- Entity销毁时哪些关系要跟着销毁，比如角色销毁，技能、物品、武器、子弹、特效要跟着销毁
- Windsurf自动补全失效
- SKILL优化，老是不触发
- 系统开启/关闭统一管理，现在系统都是用AutoLoad自动开启，如何快速设置，比如在Data\Config生成C#文件直接设置
  - 还有就是系统能否在游戏运行过程中轻松开启和关闭？


## FeatureSystem
- @Data/Data/Feature @Src/ECS/Base/System/FeatureSystem @Src/ECS/Base/System/AbilitySystem @Data/Data/Ability @CircleDamage.cs @AbilityConfig.cs 
AbilityFeatureHandlerBase现在有FeatureId和FeatureGroup，我之前本来只想保留一个FeatureId，因为如果只是索引，那只需要FeatureId就够了，比如CircleDamage的FeatureId => global::FeatureId.Ability.Passive.CircleDamage;  
如果一个技能对应一个FeatureHandler这样就够了
但是要做模板技能，也就是FeatureHandler要复用给其他，其实FeatureHandler不局限于技能，它是一个通用的执行函数，可以用在ability,buff,item等地方，我在想要不要把AbilityFeatureHandlerBase干掉，直接对接IFeatureHandler，反正OnExecute那些判断可有可无，这样的话IFeatureHandler写法都很统一了，需要对应的数据只需要FeatureContext.ActivationData就能获取
然后是FeatureHandler复用的问题，只需要根据FeatureId找就行了，感觉FeatureGroup也没什么必要
实际上技能AbilityConfig现在的参数不对，少了，1.Name技能名称，2.技能分组，3.FeatureHandler 。1和2都是技能的信息，3是执行哪个FeatureHandler，然后通过设置DataKey就设置了对应模板技能的参数
然后是FeatureSystem的生命周期，Granted/Removed、Enabled/Disabled没问题，但是Activated → Execute → Ended为什么要3个？我的想法是只保留Execute就行了？你去广泛搜索其他游戏框架或者相关的内容
深度思考详细分析，广泛搜索web,map



## 移动系统
- Godot Tween是否能替代我现在的运动策略，为什么
  

## 伤害
- 碰撞后一次伤害/多次伤害，伤害间隔

## TodoList



- [] 详细查看数字飘字逻辑

- [] DataKey检查，分多过多的分多一个文件
- [] 事件检查

- 表格里面不能填具体伤害，比如我想填技能伤害=防御力*3+10，这个应该怎么做？或者说这个应该算逻辑不是数据？



## 大方向
- []特效系统
- []物品系统
- []BUFF系统
- []三选一玩法

## C#表格插件

@Data\DataNew @addons\DataConfigEditor @docs/框架/文档/Data数据/纯CSharpData存储方案.md

  现在没有分组，但分组显示得不明显，没有放在一起，分组需要可以隐藏不看，也可以打开；不能批量修改同一列的值，不能Ctrl多选，之前说三行的，现在只保留一行就行了，因为照片里面的字段包含中文注释，因为C#代码里面本来就有注释的
    重要的一点：能不能预览图片、场景等的图片，有张图片显示我就知道这是什么图片，不然只有路径我哪里知道
  深度思考详细分析广泛搜索，反正最后要做出一个功能完善好用的C#数据管理插件，我现在是先做插件看看效果，
  godot启动的时候时间边长了，就是因为这个，想想怎么优化
  
@Docs/其他/记录/image-6.png @AbilityConfigData
    第一行的格子的DataKey英文+中文注释两行没有做好，比如AbilityConfigData里面每个字段都有注释，对照不就行了吗？拿不到吗？summary。
  enum显示错位，很多时候点击表格enum的位置的时候选项出现在左上角而不是选项的位置，而且enum的显示没有我想的好，点击表格上enum的格子后出现选项，选项的名字要注意：左边英文，右边注释中文显示
  ，跟enum定义一样的

---
```markdown
@addons/SlimeDataTable_CSharp @Data/Data @Tools/ResourceGenerator  
现在数据存放是在Data\Data路径，用config.cs代码作为模板，然后根据这个代码模板生成godot场景，但是.trres和C#不互通，就比如Dash.cs和DashConfig.tres，本来可以根据Ability.Movement.Dash完成分组，一个变量搞定，现在多了个FeatureGroup就是因为不互通，现在分成两个其实维护起来也不好，如果Dash技能的分组变了，Dash.cs和DashConfig.tres都要跟着变，如果都能获取C#或者数据直接用C#而不是.tres，就能一改就全改，问题在.tres不互通
GDS表格插件也有问题，不能用继承，比如enemyconfig继承unitconfig，但是enemyconfig不能重置unitconfig的属性，就是因为GDS表格插件有问题才想着要改
我以前也改过纯C#的方案，后面又改回.tres了，根本原因是godot引擎支持+可视化UI，一个是因为resources_table插件现成的能够直接显示表格，不过挺僵硬的，没有excel那样好用，优点是.tres在godot中能够用检查器
我现在想改回纯C#，在Data\DataNew路径单独生成，迁移，不影响当前设置，纯C#而不是.tres。
- 难点1：与godot场景兼容问题，比如AuraShieldConfig.tres，.tres包含场景信息，而C#似乎不包含，其实传路径也可以，ResourcePaths.cs是Tools\ResourceGenerator工具生成的，自动更新，可以传路径，解决了不自动更新的问题，AbilityConfig.cs的EffectScene、ProjectileScene都是PackedScene，对比字符串路径的好处是能够在godot中拖拽，这个是否能兼容，如果不能，那我把文件拖拽进去要替换字符串路径才行，这样也可以，实际上字符串路径更好，不需要跟godot场景兼容，是最好的方式，最简单，也容易实现，游戏数据管理肯定不会说用个场景放进去的，这样更好
- 难点2：UI显示，之前用tres主要是因为能够使用Godot的UI，能够直接显示，但是Godot的UI本身就是一种限制，应该没有直接用.net生态做出来的软件UI更好。其他AI我不打算做这个项目，但你是GLM5.1，在长任务方面优势明显，你就算是写一个C#版的excel那样好用的东西我觉得是可行的，可以尝试写一个C#版的表格软件，完全兼容C#和Godot
- 主要抉择点：使用GodotUI还是完全不用Godot，我之前纯C#就是这么想的，效果还行，之前没这么做是感觉做个软件比较麻烦，现在可以尝试
- 要不要用ResourceGenerator作为更新路径的手段？但是这样的话有点奇怪，如果说要做软件的话，为什么还需要这个小工具？还是说在软件内部自动检查路径是否有问题，如果是空路径（一般直接移动路径就会出现）就在表格软件上直接显示出来
- 做表格软件需要做大量的搜索，网上会有很多轮子，不一定需要自己做，仔细搜索，同时也学习别人优秀的方案怎么实现的
深度思考详细分析广泛搜索，必须仔细查看godot文档和使用filesystem mcp查看godot源码(我clone到本地了：/mnt/e/Godot/GodotEngine/godot路径)，广泛查询网上
详细分析深度思考这种改法是否能够完美实现，
- 我肯定有很多地方表达不清晰，你在网上详细搜索相关资料仔细研究后跟我详细说明，不清晰的地方跟我交流
```

## 完成基本Demo后有待完善
- []AI系统检查
- AI新加的东西检查，TargetSelector检查
- []Data事件、DataKey的规范写到Skill
- []ComponentPreset用代码代替场景是否更好？相关的Component放在一起，比如位置目标技能的右摇杆移动组件应该跟技能相关组件放一起
- []检查Datakey和Datakeyregister

## 有待解决
- Data系统、伤害系统增伤易伤区分
- 护盾、Buff、Effect系统
- Docs\ECS\修改\有待修改.md

- Tween 动画做成工具？有Timer有必要用Tween吗？





