
### 1
@TestSystem @FeatureSystem
- TestSelectionContext.Events应该放在TestSystem，专门是TestSystem的事件
- TestSystem.RegisterModulesFromScene排序是否有必要？我认为安装字母名称顺序随便排序或者不排序都行
- 现在要给TestSystem的测试模块分组，测试模块里面写分组，属于哪个分组，比如测试系统可以分属性、技能分组，分组可以多重嵌套，分组跟FeatureId_Ability一样，比如技能.测试1.测试2，那就是技能是第一分组，测试1是第二分组，测试2是测试模块名字，新建个字段在ITestModule里面，参考FeatureSystem，根据这个自动在UI里面分组
- TestSystem选择测试模块下拉列表太长了，也要分组，深度思考怎么做，我认为选择测试模块的UI要重构，可以放在左边，可以随时隐藏，需要的时候才显示，深度思考不一定我说的这样，反正最终结果要我满意，要符合常理，继续放上面也是可以的，反正分组要做好，要自然
- ITestModule我很奇怪，位深不直接用FeatureSystem的生命周期统一管理，而是自己写新的，重复造轮子
- 刷新调度有关逻辑过度设计太过复杂都不知道在写什么，需要刷新直接刷不就好了？搞那么多干什么，TestModuleBase、TestRefreshScheduler等
- 以资深游戏开发者深度思考详细分析，广泛搜索web,mcp，其他游戏框架只是参考，不能照抄，要适合当前GodotC#ECS框架

### 2
- Src/ECS/Base/System/TestSystem Src/ECS/Base/System/Spawn Src/ECS/Test/SingleTest/ECS/System/Spawn Data/ResourceManagement/ResourcePaths.cs Data/Data/Unit
  新加一个可以自定义生成敌人的测试，我认为可以参考SpawnSystem测试场景，要可以选择单位，Data里面有单位的数据.tres，深度思考怎么做
  其实获取所有当前系统的技能、单位、特效等这个是一个通用的，看看要做成什么工具，比如War编辑器里面就是可以选择不同类型的单位，我感觉可以通过获取tres数据然后分析获得所需的所有单位/特效/技能
  等，然后通过数据进行分类，选择之类的，深度思考详细分析广泛搜索,web,mcp
  使用superpowers详细规划，搜索可以放到subagent,只提取核心信息关键结论节省token
---
$brainstorming  $using-superpowers
Src/ECS/Base/System/TestSystem   Data/ResourceManagement
- 测试系统新增查看对象池信息测试模块，分组是信息，可以查看对象池详细信息，可以详细到某个池的信息，当前数量、最大数量等参数
- 深度思考详细分析有什么测试信息是符合本项目框架适合让开发者在游戏中查看的
- 我想做一个能够批量查看视觉场景CharactorBody2D的测试模块，我不确定要不要做成测试模块，还是说单独的测试场景，用途：将某个类型，比如单位、特效，可以按照分组筛选，比如可以选单位-玩家，单
  位-敌人等，反正分组之前应该有做的，Data/ResourceManagement，这个是用来将这些视觉场景实例化，放在地图上查看效果，可以统一播放某个动作，看看实际效果怎么样，这个用途应该很常见，或者做成
  插件更好？
-

### 4
- $using-superpowers
  Src/ECS/Base/System/TestSystem/VisualPreview Docs/框架/ECS/System/TestSystem/优化/TestSystem新增对象池信息与单位Asset视觉预览.md Data/ResourceManagement/ResourcePaths.cs Data/
  ResourceManagement/ResourceCategory.cs Src/ECS/Base/Component/Unit/Common/UnitAnimationComponent
  视觉场景预览需要完成重构，不能再放在TestSystem了，需要单独放在Src\ECS\Test\GlobalTest中，其实所谓视觉场景预览就是将这些视觉场景CharactorBody2D,Sprite2D实例化到地图上而已，每个场景间隔一
  定位置，横竖排列，然后这些场景统一播放动作，如果某个场景没有当前播放的动作，就改回默认状态或者暂停，这里需要思考怎么样处理没有这些动作的场景，思考是用UnitAnimationComponent管理播放动作
  还是不用，用的话还是实例化entity了，反而麻烦了，应该不要用，反正有些逻辑单独写也不难
  场景的路径和分类统一由ResourcePaths和ResourceCategory为准，左边要有UI选择分类，选择某个分类就只显示这个分类下的场景，没选到的分类的场景隐藏不显示即可，反正这里只需要检查视觉效果
  深度思考详细分析广泛搜索web,mcp，注意搜索资料可以在subagent，把重点放回上下文即可
- 要支持鼠标选择，这样的话应该要用Entity，用TargetingIndicatorEntity就行，没有多余component，把视觉场景加上去就行，就能显示出不同的视觉场景，鼠标选择后要显示这个Entity的名字，视觉场景路径这些必要的信息，DataKey.Name要改成视觉场景的名字，也就是ResourcePaths路径最后场景名称

### 

### 5
Data/Data/Ability/Ability/Movement/ArcShot @AbilitySystem @AbilityTargetSelectionComponent @AbilityConfig @Data/Data/Ability
  [20:21:41][DEBUG][ActiveSkillInputComponent] 切换主动技能: 圆弧射击 (索引: 1)
  [20:21:41][DEBUG][ActiveSkillBarUI] 收到 ActiveSkillSelected 事件: Index 1, Name: 圆弧射击
  [20:21:45][DEBUG][AbilitySystem] 目标验证失败: 圆弧射击 需要 Entity 目标但未找到
  [20:21:45][DEBUG][ActiveSkillInputComponent] 技能触发失败: 圆弧射击
  [20:21:45][DEBUG][AbilitySystem] 目标验证失败: 圆弧射击 需要 Entity 目标但未找到
  [20:21:45][DEBUG][ActiveSkillInputComponent] 技能触发失败: 圆弧射击
  [20:21:46][DEBUG][AbilitySystem] 目标验证失败: 圆弧射击 需要 Entity 目标但未找到
  [20:21:46][DEBUG][ActiveSkillInputComponent] 技能触发失败: 圆弧射击
  ArcShot点击后没反应
  AbilitySystem有问题，AbilityConfig的目标选择分组要精简，只保留AbilityTargetSelection、AbilityCastRange、AbilityEffectRadius，AbilitySystem用AbilityTargetSelectionComponent还有AbilityConfig的参数提前选目标是完全错的，选目标应该在具体技能逻辑里面去选，如果觉得这个否成冗余每次都要写可以写个工具类简化，提前选目标很容易错，这本来就应该是AbilityHandler里面要做的而不是这里做的，AbilitySystem也要兼容，在AbilityHandler执行之前允许目标为null，这是正常的
  相关的地方要修改，包括技能资源数据，还有所有的AbilityHandler
  深度思考详细分析广泛搜索







## 资源预览

- @image-3 
预览资源测试场景的动画没有循环播放，按刷新全部改回初始状态了体验很不好，选择动画最好支持鼠标滚轮选择，鼠标选择entity信息要跟其他信息间隔一点距离