### System-20260422
#### 1
- 使用superpower
  @Src/ECS/Base/System/Core/ @docs/框架/ECS/System/Core/ @Data/Config/System/
  系统Profile改来改去没改好，Profile本意是对每个系统增加信息说明这个系统是否运行，是否添加，因为我这个框架是通用的，只需要添加移除一些系统就是另外一个游戏，系统不断做优化迭代越做越好，这
  是本意，系统需要额外添加的信息：是否自动加载，是否允许运行，分组，TAG（标签的话应该更加灵活）
  有些系统是必须打开的，是基础的系统
  可以预设某些游戏类型，打开什么系统，我一设置这个游戏类型就把这些游戏打开，当然这个游戏类型怎么设置系统，我觉得要支持用分组/标签来开启对应的系统这样比较方便，其实分组和标签应该可以合成一个分组，一个系统可以是多个分组的，为什么要限制一个呢？
  系统管理需要很轻松地获取当前所有系统，因为需要debug查看信息，需要很轻易地按照分组筛选分组，或者查看所有分组，知道我开启了哪些系统，这个是TestSystem需要做的
  需要支持表格统一编辑所有表格的信息，这个用于切换游戏时快速切系统，哪些启用哪些不启用
  我认为一个SystemDescriptor就够了，系统信息都放这里，不要额外管理其他了，冗余
  深度思考详细分析，广泛搜索web,mcp参考相关成熟引擎和框架的做法
  我有表达不清晰的地方要马上跟我交流确认补充，不可乱来，我只说了想法肯定还有很多东西没确认
  这是复杂任务，需要分步骤执行，一步完成再走下一步，需要生成执行方案文档，在docs/框架/ECS/System/Core/路径
  你是什么模型
---
#### 2
- SystemManager的SystemLifetime似乎没什么实际逻辑，只是挂载在不同Node而已，完全用多分组替代 SystemLifetime，不过多分组又有个问题，按照哪个分组创建Node，我觉得按照第一个分组，系统自觉设置分组，如果没设置就加入Else分组
- 你要思考一个问题，是用一个分组还是分组+TAG，如果两个的话分组可以作为挂载Node的路径，而TAG可以作为游戏按照TAG批量开启系统，深度思考，一旦确认，方案所有分组/TAG的就固定不要再改了
- 分组/TAG用Enum位操作，这样就可以多选
- 必须打开的基础系统：这个简单，分组/TAG加个Base分组就行，基础分组直接加载不就行了，基础分组的系统暂时不建议移除，在文档说明，如果后续要改再作修改
- 游戏类型预设GameTypePreset，支持设置分组/标签，也支持直接列举系统 ID，注意：不要用字符串列出来，用enum，系统 ID用Nameof，不然我系统改名了，到处都要改
  - 游戏类型预设GameTypePreset应该改成SystemPreset，还有问题：
    - 1.多个SystemPreset怎么知道哪个执行哪个不执行？按哪个？
    - 2.SystemPreset如果设置了，那这个SystemPreset下的分组/标签对应的系统添加，这个是优先执行的，然后再看表格里面哪个系统设置了默认加载，就加载，最后把需要加载的系统一起加载
- 注意分组和TAG只是确认自动加载，不保证默认是否enable，只保证这个系统添加进去，当然绝大多数系统都是默认enable
- 所谓Profile现在只不过是补充信息，既然表格已经有了这些信息，完全去掉也没问题
- 有个问题，系统的数据来源有两个，一个是SystemRegistry.Register注册的时候输入的，一个是Data/Config/System/路径下的资源表格设置，我认为SystemRegistry.Register传入的SystemDescriptor能简单就简单，Data/Config/System/ 这里的表格设置作为主要，表格最直观，改起来最容易，而且数据逻辑分离，真正的逻辑放在Data/路径最好
- 表格统一编辑的问题，是用Config.cs+.tres+resources_table插件，Data/Data就是这么用的
- 完成任务后要更新所有相关文档
- 有问题继续交流

#### 3
- 用分组（挂载） + TAG（逻辑分类）
- SystemRunCondition现在是一个大类，我的意思是系统也做成是数据去掉的，以.tres的表格数据导入，SystemRunCondition不过是设置了几个Allowed、Blocked 4个Phase，完全可以做成enum按位选
  - 总的来说SystemDescriptor除了Factory都要支持表格写入，系统注册SystemRegistry时只填SystemId和Factory即可，其他表格填入，这样就是数据驱动，跟现在ECS框架的数据驱动理念一致
  - 还有SystemPreset也要放到Data/Config/System/路径下，也是数据驱动
  - SystemPreset也是用表格也是Config.cs+.tres。
  - SystemPreset做成表格是有多个的，还要额外加个参数说明这个SystemPreset是否启用，一个SystemPreset可能就是一个游戏类型，所以要一键在表格启用/禁用
- 测试系统是最后做的，在更新文档之后，测试系统可以参考技能测试，不过要优化，因为除了分组还有个Tag，要支持分组+TAG筛选，左边显示所有的系统，按照分组+TAG筛选，分组+TAG筛选是两个维度可以重合，分组+TAG筛选放在上面，左边共用分组+TAG+add,remove,enable,disable筛选。左边显示系统筛选后的所有系统，add,remove,enable,disable的系统分别不同颜色，用适合的UI显示。鼠标选择这个系统，右边显示这个系统的信息，信息在对应系统逻辑里面获取
  - 系统信息应该加到系统ISystem对应的地方里面，是系统统一的重要一步，是测试的重要一步，这个可以先做
- Groups挂载路径按照SystemGroup从小到大为优先顺序，不是第一个，比如Base的优先级是最高的，也是自动加载的，外面想禁用也禁用不了，这个补充到文档，所以Base要谨慎
- resources_table 插件很明显是每个系统一个文件，每个文件就是表格一行
- 其他问题：resources_table插件不支持enum来按列筛选，现在只支持按列的字母名字顺序来筛选，有点简陋，要改，TODO，放在TestSystem计划后面
- 有问题继续交流，应该基本上很多问题都清楚了

#### 4
- SystemRuntimeInfo GetRuntimeInfo()跟IProjectStateAwareSystem应该可以整合成一个
- 系统运行时信息你误会了，当然你提到的这些是对的，这些是系统的信息，还需要支持自定义函数获取具体系统的信息，比如SpawnSystem、伤害系统（总伤害之类的）、恢复系统（有多少个单位恢复）就可以显示一些统计信息帮助开发者更好地查看，所以要支持自定义函数获取信息
  - 不过现在很多系统没有写获取信息，这个也是TODO，放在方案后面
- TestSystem UI的Bool类型可以用CheckButton
- 多个ActivePreset暂时只支持一个，在文档说明
- 注意README文档要精简一点，主要说明这个系统怎么用，Doc/的文档可以详细说明原理
- Base 分组的系统不允许 Remove，也是写入文档
- 还有问题跟我交流，只列出问题即可

#### 5
- SystemConfig 和 SystemPreset肯定是不同文件夹不同Config.cs的，同一个SystemConfig不用.tres可以分文件夹也可以不分
- Tools\ResourceGenerator要更新路径和分组
- ISystem 接口整合你误解了，ISystem继承ISystemLifecycle+另外一个接口B，接口B放OnProjectStateChanged、GetSystemRuntimeInfo，这个意思，这三个接口文件可以放到一个新的文件夹
- 自定义系统信息的接口设计暂时用方案 B：强类型扩展
- Phase 枚举的预设组合可以加，比如ProjectStateService就有很多写好的组合，直接增加enum就行，直接选这个，不用在代码里面预设了，可以在Data/Config/System/对应路径加个README文档
- SystemConfig 的 Dependencies 字段类型，加一个要手动维护的所有系统名字的enum，不单单是Dependencies要用，这个enum是通用的
- 还有问题跟我交流，只列出问题即可

#### 6
1. SystemConfig 表格路径，方案 B（分文件夹），路径：Data/Config/System/System/
2. SystemPreset 路径:Data/Config/System/SystemPreset/
3. SystemId
4. Phase 组合枚举的位置这样就行
5. 接口 B 的命名ISystemFunction
6. 新接口文件夹的命名ISystem

#### 7
- ISystemRuntime好像是以前的写法，现在没改过来，SystemManager也是错了没改过来，ISystemLifecycle才是真正的生命周期管理，系统继承ISystem是调用这里的，SystemManager也要用这里才对，ISystemLifecycle应该删掉
- SystemRuntimeInfo是跟着GetSystemRuntimeInfo的，GetSystemRuntimeInfo放来放去放不对，直接放ISystem
- SystemManager的_hosts[SystemGroup.Base] = EnsureHost(SystemGroup.Base);难道不能写个循环遍历SystemGroup所有吗？写那么多个





